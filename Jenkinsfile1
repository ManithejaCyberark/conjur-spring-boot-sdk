node{
    stage("maven version"){
        def mavenHome =  tool name: "maven", type: "maven"
      def mavenCMD = "${mavenHome}/bin/mvn"
       sh "${mavenCMD} --version"
       sh "${mavenCMD} clean package"
    }
}
